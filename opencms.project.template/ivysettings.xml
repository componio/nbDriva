<ivysettings>
    <!-- Default ivysettings.xml stuff -->
    <include url="${ivy.default.settings.dir}/ivysettings-public.xml" />
    <include url="${ivy.default.settings.dir}/ivysettings-shared.xml" />
    <include url="${ivy.default.settings.dir}/ivysettings-local.xml" />
    <include url="${ivy.default.settings.dir}/ivysettings-main-chain.xml" />
    <include url="${ivy.default.settings.dir}/ivysettings-default-chain.xml" />
    <!-- Customized stuff -->
    <settings defaultResolver="public" />
    <resolvers>
        <ibiblio name="opencms.shell.extension" root="https://raw.github.com/tpinky/shellDriva/master/lib-repo/" m2compatible="true"/>
    </resolvers>
    <modules>
      <module organisation="net.componio" resolver="opencms.shell.extension"/>
    </modules>
</ivysettings>